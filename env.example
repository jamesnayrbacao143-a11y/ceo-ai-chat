# GitHub AI API Configuration
# Get your Personal Access Token (PAT) from GitHub settings
# Create your PAT token: https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/managing-your-personal-access-tokens
GITHUB_TOKEN=your_github_pat_token_here

# Optional: Single model (for backward compatibility)
# Available models: openai/gpt-4o, openai/gpt-4o-mini, openai/gpt-4-turbo, openai/gpt-3.5-turbo,
#                   anthropic/claude-3-5-sonnet, anthropic/claude-3-opus, anthropic/claude-3-sonnet, anthropic/claude-3-haiku,
#                   meta/llama-3.1-405b, meta/llama-3.1-70b, meta/llama-3.1-8b,
#                   google/gemini-2.0-flash-exp, google/gemini-1.5-pro, google/gemini-1.5-flash
GITHUB_MODEL=openai/gpt-4o

# Optional: Multiple models for Auto mode (comma-separated)
# If GITHUB_MODELS is set, it will override GITHUB_MODEL
# Format: model1,model2,model3
# These models will be used in Auto mode (balanced quality and speed, recommended for most tasks)
# Example: openai/gpt-4o,openai/gpt-4o-mini,anthropic/claude-3-5-sonnet
# GITHUB_MODELS=openai/gpt-4o,openai/gpt-4o-mini,anthropic/claude-3-5-sonnet

# Optional: Set a custom port (default is 3000)
# PORT=3000

# Bytez Image Generation (text-to-image)
# Get your key at: https://bytez.com/settings
BYTEZ_API_KEY=your_bytez_api_key_here

# Optional: override the default Stable Diffusion model used for image generation
# BYTEZ_IMAGE_MODEL=stabilityai/stable-diffusion-xl-base-1.0

# Optional: override Bytez chat models
# BYTEZ_GPT4O_MODEL=openai/gpt-4o
# BYTEZ_GPT4O_MINI_MODEL=openai/gpt-4o-mini
# BYTEZ_GPT41_MODEL=openai/gpt-4.1

# PostgreSQL Database Configuration (Supabase)
# For Vercel: Use Supabase (recommended), Railway, or AWS RDS
# 
# ⚠️ IMPORTANT: For Vercel serverless functions, use SESSION POOLER (port 6543), not Direct connection (port 5432)
# Get connection string from Supabase: Settings → Database → Connection string → Session Pooler → URI
# 
# Session Pooler (RECOMMENDED for Vercel):
# Format: postgresql://postgres:password@db.xxxxx.supabase.co:6543/postgres?pgbouncer=true
DATABASE_URL=postgresql://postgres:your_password@db.xxxxx.supabase.co:6543/postgres?pgbouncer=true
#
# Direct connection (NOT recommended for Vercel - IPv4 incompatible):
# Format: postgresql://postgres:password@db.xxxxx.supabase.co:5432/postgres
# DATABASE_URL=postgresql://postgres:your_password@db.xxxxx.supabase.co:5432/postgres
#
# Alternative: Use POSTGRES_CONNECTION_STRING instead
# POSTGRES_CONNECTION_STRING=postgresql://postgres:password@db.xxxxx.supabase.co:6543/postgres?pgbouncer=true

# Individual PostgreSQL settings (optional, if not using connection string)
# POSTGRES_HOST=db.xxxxx.supabase.co
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=your_password
# POSTGRES_DATABASE=postgres
# POSTGRES_PORT=5432
# POSTGRES_SSL=true

# JWT Secret for authentication tokens
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Email Configuration (for verification)
# Gmail example:
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASSWORD=your_app_password
EMAIL_FROM=noreply@harviongpt.com

# App URL (for email verification links)
APP_URL=http://localhost:3000

# Google OAuth Configuration
# Get credentials from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/google/callback
# For production, update GOOGLE_REDIRECT_URI to your production URL